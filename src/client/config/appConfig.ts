// Committed frontend runtime config.
// Only place public frontend-safe values here (never service role keys).
export const appConfig = {
  supabaseUrl: 'https://YOUR_PROJECT_REF.supabase.co',
  supabaseAnonKey: 'YOUR_SUPABASE_ANON_KEY',
  apiBaseUrl: '',
};
